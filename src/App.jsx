/* eslint-disable*/
import React, { PureComponent } from 'react';

import Root from './root';

class App extends PureComponent {
  render() {
    return (
      <Root />
    );
  }
}

export default App;
